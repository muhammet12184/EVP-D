name: obd_diagnostics_enhanced
description: OBD Diagnostics with enhanced features - Fuel consumption, Regen braking, Vehicle detection
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Date formatting
  intl: ^0.18.0
  
  # State management (optional - uncomment if needed)
  # provider: ^6.0.0
  # riverpod: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
